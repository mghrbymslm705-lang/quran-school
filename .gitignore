# التبعيات والمخرجات
node_modules
dist
*.log
*.err

# أسرار وإعدادات البيئة — لا تُرفع أبدًا إلى المستودع
.env
.env.*
!.env.example

# قواعد البيانات والنسخ الاحتياطية — بيانات حساسة محليًا
*.db
*.db-journal
*.sqlite
*.sqlite-journal
backups

# ملف إصدار البناء (يُولَّد أثناء البناء)
version.json
