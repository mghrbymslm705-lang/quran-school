# تقرير المرحلة التاسعة — تهيئة بيئة إنتاج مستقلة وآمنة

**التاريخ:** بعد إكمال المراحل 1–8 بنجاح.
**النطاق:** فصل بيئة الإنتاج عن بيئة التطوير دون لمس الثانية، مع آلية إعداد أولي آمنة ومنع بيانات تجريبية في الإنتاج.

---

## أولًا: ما الذي فُحص قبل التعديل؟

### بنية المشروع
- خادم خلفي في `server/` (Node.js + Express + `node:sqlite` المدمج).
- واجهة أمامية في الجذر (Vite + React + TypeScript).
- الاختبارات في `server/test/` (node:test)، وتُشغَّل بـ `DB_PATH` يشير لقاعدة مؤقتة.

### طريقة الاتصال الحالية بقاعدة البيانات
كانت في `server/src/db.js`:
```js
const DB_PATH = process.env.DB_PATH || path.join(__dirname, '..', 'school.db')
export const db = new DatabaseSync(DB_PATH)
```
أي أن الكل (تطوير/اختبار/إنتاج) كانوا يشيرون إلى نفس الملف `school.db` في حال عدم تمرير `DB_PATH`.

### طريقة تشغيل seed
- `server/src/server.js` كان يستدعي `seed()` دون قيد عند كل إقلاع.
- `server/src/seed.js` يُدرج: `admin/admin123`، `teacher1/2/3`، حلقات `G1/G2/G3`، طلاب `S001–S010`، وسجلات يومية تجريبية — ويطبع كلمات المرور في السجل.
- `package.json` كان يحتوي على `"seed": "node --experimental-sqlite src/seed-run.js"` لكن الملف `src/seed-run.js` **غير موجود** (سكربت معطوب).

### طريقة النسخ الاحتياطي الحالية
- `server/scripts/backup.js` كان ينسخ ملف القاعدة عبر `fs.copyFileSync` مباشرة (غير آمن أثناء العمل)، ويشير إليه `package.json` بمسار خاطئ `src/scripts/backup.js` (مع أن الملف في `scripts/backup.js`).
- لم يكن يميّز بين بيئتي التطوير والإنتاج.

---

## ثانيًا: ما الذي تم تعديله؟

### ملفات جديدة
| الملف | الوصف |
|---|---|
| `server/src/db-path.js` | يحدد مسار القاعدة ومجلد النسخ حسب `NODE_ENV` + قارئ `.env` بسيط وآمن (دون اعتماد خارجي). |
| `server/src/seed-run.js` | إصلاح سكربت `npm run seed` المعطوب (يستدعي `seed()`). |
| `server/scripts/setup-production.js` | إعداد أول مشرف في الإنتاج بشكل آمن (مطالبة باسم مستخدم وكلمة قوية، تشفير bcrypt، لا كلمة افتراضية، لا طباعة أسرار). |
| `server/.env.example` | نموذج إعدادات البيئة (بدون أسرار حقيقية). |
| `server/.gitignore` | يستبعد `.env` و`backups/` و`*.production.db` و`school.db`. |
| `server/test/phase9.test.js` | اختبارات المرحلة التاسعة (١٠ اختبارات). |

### ملفات مُعدَّلة
| الملف | التعديل |
|---|---|
| `server/src/db.js` | يستخدم `resolveDbPath()` بدل المسار المضمَّن. |
| `server/src/server.js` | `seed()` يُستدعى فقط إن `NODE_ENV !== 'production'`. |
| `server/src/seed.js` | حارس أمان: يرفض العمل في الإنتاج ويتوقف برسالة واضحة. |
| `server/scripts/backup.js` | إعادة كتابة: `VACUUM INTO` (نسخة متسقة وآمنة أثناء العمل)، مجلد منفصل لكل بيئة، تصدير دوال قابلة للاستدعاء من الاختبارات. |
| `server/package.json` | إصلاح مسار `backup`، إضافة `setup-production`، وإضافة `--experimental-sqlite` الضروري للسكربتين (لأنهما يستخدمان `node:sqlite`). |

**لم تُمس:** منطق النظام والصلاحيات، بيانات التطوير (`school.db`)، الاختبارات القديمة، ملفات المشروع الأخرى.

---

## ثالثًا: كيف أصبح التشغيل؟

### وضع التطوير (الموجود سابقًا — لم يتغير سلوكه)
```
cd server
npm run dev        # أو npm start
```
يستخدم `school.db` (نفس القاعدة التجريبية)، ويُنشئ بيانات seed تلقائيًا عند أول تشغيل.

### وضع الإنتاج
يُحدَّد عبر متغير البيئة `NODE_ENV=production`. يختار النظام تلقائيًا قاعدة مستقلة `school.production.db` (أو `DATABASE_PATH` إن وُجد). يُمنع seed تمامًا.
```
cd server
# عبر .env فيه NODE_ENV=production، ثم:
npm start
```
أو صراحةً:
```
NODE_ENV=production npm start
```

### الإعداد الأول للمشرف (آمن)
مرة واحدة فقط، قبل أو بعد تشغيل الخادم:
```
NODE_ENV=production npm run setup-production
```
يطالب باسم مستخدم وكلمة مرور قوية (٨+ خانات وأحرف وأرقام)، ويخزّنها مشفَّرة بـ bcrypt. لا يُنشئ أي حساب افتراضي. للأتمتة يمكن تمرير:
```
NODE_ENV=production SETUP_USERNAME=admin SETUP_PASSWORD='كلمة-قوية' npm run setup-production
```
يدعم إدخالاً تفاعليًا بكلمة مرور مُخفاة (`*`) ولا يطبع أي سر.

---

## رابعًا: قاعدة البيانات

- **مسار التطوير:** `server/school.db` (لم يُمس، بياناته التجريبية باقية).
- **مسار الإنتاج:** `server/school.production.db` (افتراضيًا) أو ما يحدده `DATABASE_PATH`.
- **منع الخلط:** الحل عبر `resolveDbPath()`:
  - `DATABASE_PATH` أولوية قصوى (تجاوز صريح، تستخدمه الاختبارات عبر `DB_PATH` أيضًا).
  - `DB_PATH` اسم قديم للتوافق مع الاختبارات.
  - `NODE_ENV === 'production'` ← `school.production.db`.
  - غير ذلك ← `school.db`.
  - قارئ `.env` يملأ المتغيرات فقط إن لم تكن معرَّفة (لا يطغى على إعدادات الاختبارات).
- **مخطط الإنتاج كامل:** عند فتح قاعدة الإنتاج يُنفَّذ نفس `SCHEMA` + التهجيرات + الفهارس، فتصبح القاعدة **نظيفة من البيانات لكن مخططها كامل** (كل الجداول، القيود، الفهارس، آليات الصلاحيات).

---

## خامسًا: النسخ الاحتياطي

- **الأمر:** `npm run backup` (أو `node --experimental-sqlite scripts/backup.js`).
- **الاستعادة:** `node --experimental-sqlite scripts/backup.js restore <ملف>`.
- **أين تُحفظ:** `server/backups/production/` في الإنتاج، و`server/backups/development/` في التطوير — مجلدات منفصلة تمنع الخلط.
- **التسمية:** `school-YYYYMMDD-HHMMSS.db` (تاريخ ووقت).
- **الأمان:** النسخة تُنشأ عبر `VACUUM INTO` (لقطة متسقة حتى أثناء الكتابة على القاعدة المفتوحة)، ولا تُطبع أي بيانات حساسة (أسماء الملفات فقط).
- **تم اختباره فعليًا:** نُفِّذ ضد قاعدة إنتاج حقيقية وأنتج ملفًا صالحًا في `backups/production/`.

---

## سادسًا: الاختبارات

### اختبارات المرحلة التاسعة (`phase9.test.js`) — ١٠/١٠ ناجحة
1. `development` يستخدم `school.db`.
2. `production` يستخدم `school.production.db` المستقلة.
3. الإنتاج لا يحتوي بيانات seed تلقائيًا.
4. `seed` يرفض العمل في الإنتاج.
5. لا يوجد حساب `admin` افتراضي في الإنتاج.
6. قاعدة الإنتاج تحتوي على جميع الجداول اللازمة.
7. قاعدة الإنتاج تحتوي على الفهارس اللازمة.
8. النسخ الاحتياطي يستهدف مجلد الإنتاج `backups/production`.
9. فشل الإعداد في غير الإنتاج لا ينشئ/يحذف قاعدة التطوير.
9ب. الإعداد الآمن ينشئ مشرفًا بكلمة قوية (مشفَّرة، لا نص صريح).

### نتيجة جميع الاختبارات السابقة
- صلاحيات: ١١/١١ · المرحلة ٣: ٧/٧ · المرحلة ٤: ٢٠/٢٠ · المرحلة ٥: ٢٤/٢٤ · المرحلة ٦: ٢٦/٢٦ · المرحلة ٧: ٣٠/٣٠ · المرحلة ٨: ٢٢/٢٢ · المرحلة ٩: ١٠/١٠.
- **الإجمالي: ١٥٠/١٥٠ ناجحة.**
- `tsc`: خروج ٠ (لا أخطاء أنواع).
- `vite build`: نجح بلا تحذيرات.

---

## سابعًا: اختبار عملي (حقيقي لا قراءة كود فقط)

1. **تشغيل إنتاج نظيف:** خادم على المنفذ ٤٠٠٠ مع `NODE_ENV=production` وقاعدة إنتاج مؤقتة فارغة.
2. **عدم وجود بيانات تجريبية:** بعد الإقلاع لم توجد `admin/teacher1/S001`.
3. **إنشاء أول مشرف آمن:** `npm run setup-production` باسم `liveadmin` وكلمة قوية → نجح، وبالتدقيق في القاعدة كانت كلمة المرور **مشفَّرة (bcrypt)** لا نصًا صريحًا.
4. **تسجيل الدخول:** `liveadmin` → ٢٠٠ (رمز JWT صالح).
5. **إنشاء حلقة:** `POST /api/groups` → ٢٠١.
6. **إنشاء معلم:** `POST /api/teachers` → ٢٠١، ثم إسناده للحلقة عبر `PUT /api/groups/:id` → ٢٠٠.
7. **إنشاء طالب:** `POST /api/students` → ٢٠١.
8. **نسخة احتياطية:** `npm run backup` → أُنشئت في `backups/production/school-YYYYMMDD-HHMMSS.db` (تحقق منها كـ SQLite صالح).
9. **بيئة التطوير لم تتغير:** فحص `school.db` أظهر بقاء `admin`, `teacher1/2/3` و١٨ طالبًا كما كانت — **لم تُمس إطلاقًا**.
10. **خصوصية المعلم:** تسجيل دخول المعلم → تسجيل يومي داخل النطاق ٢٠١، ومحظور من `/api/audit` (٤٠٣) ومن طالب خارج النطاق IDOR (٤٠٣).

---

## ثامنًا: مشاكل متبقية / ملاحظات

- **لا أعطال وظيفية.** الفصل بين البيئتين يعمل verified عمليًا.
- **نقطتان للانتباه عند النشر الحقيقي:**
  1. `JWT_SECRET` في `.env.example` قيمته `change-me-in-production` — **يجب تغييره** لقيمة سرية طويلة في الإنتاج (وإلا يمكن تزوير الرموز).
  2. الخادم يستمع على كل الواجهات افتراضيًا؛ يُنصح بتشغيله خلف وسيط عكسي (Nginx) مع HTTPS وتقييد معدل الطلبات في الإنتاج.
- **لم يُضَف جدولة تلقائية** للنسخ الاحتياطي (كما طُلب)؛ السكربت جاهز للتشغيل اليدوي الموثوق، ويمكن جدولته لاحقًا عبر `cron`/`Task Scheduler` إن رغب المشرف.
- **لم تُلمَس بيانات التطوير** ولم تُحذف، والاختبارات القديمة لم تُكسر (١٥٠/١٥٠).

---

## الحالة
**تمت المرحلة التاسعة.** بيئة الإنتاج أصبحت مستقلة، نظيفة، آمنة، بإعداد أولي بلا كلمة افتراضية، مع نسخ احتياطي معزول وآمن. بيئة التطوير باقية كما هي. لا توجد مرحلة تالية حتى توجيه جديد.
